{
  "name": "@pixel-agents/server",
  "version": "1.3.0",
  "private": true,
  "description": "Business logic: agent runtime, hook handling, asset loading, HTTP+WS server",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.1"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/static": "^9.1.1",
    "@fastify/websocket": "^11.2.0",
    "@pixel-agents/core": "workspace:*",
    "fastify": "^5.8.5"
  }
}
